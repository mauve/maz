// <auto-generated/>
#nullable enable
#pragma warning disable CS1591
using System.Text.Json.Nodes;
using Console.Cli.Http;
using Console.Cli.Shared;
using Console.Rendering;
using Azure.ResourceManager;

namespace Console.Cli.Commands.Generated;

/// <summary>Cancel a de-identification job.</summary>
/// <remarks>Cancels a job that is in progress.   The job will be marked as canceled and the service will stop processing the job. The service will not delete any documents that have already been processed.  If the job is already complete, this will have no effect. </remarks>
public partial class DeiddataCancelJobCancelCommandDef(AuthOptionPack auth) : CommandDef
{
    public override string Name => "cancel";
    protected override bool IsDataPlane => true;
    protected override bool IsDestructive => true;

    [CliOption("--deid-url")]
    public partial string? DeidUrl { get; }

    public readonly HealthDataAIDeidOptionPack DeidService = new();

    public readonly RenderOptionPack Render = new();

    /// <summary>The name of a job.</summary>
    [CliOption("--name", Required = true)]
    public partial string? ParamName { get; }

    private readonly AuthOptionPack _auth = auth;

    protected override async Task<int> ExecuteAsync(CancellationToken ct)
    {
        var client = new AzureRestClient(_auth.GetCredential(), "https://deid.azure.net/.default");
        var deidServiceBaseUrl = DeidUrl ?? (await DeidService.ResolveDataplaneRefAsync(new ArmClient(_auth.GetCredential()), ct)).ToString().TrimEnd('/');
        var path = $"{deidServiceBaseUrl}/jobs/{ParamName}:cancel";

        var result = await client.SendAsync(HttpMethod.Post, path, "2024-11-15", null, ct);
        await Render.GetRendererFactory().CreateRendererForType(typeof(System.Text.Json.Nodes.JsonNode))
            .RenderAsync(System.Console.Out, result, ct);
        return 0;
    }
}
