// <auto-generated/>
#nullable enable
#pragma warning disable CS1591
using System.Text.Json.Nodes;
using Console.Cli.Http;
using Console.Cli.Shared;
using Console.Rendering;
using Azure.ResourceManager;

namespace Console.Cli.Commands.Generated;

/// <summary>Create a priming job. This operation is only allowed when the cache is healthy.</summary>
/// <remarks>Create a priming job. This operation is only allowed when the cache is healthy.</remarks>
public partial class StoragecacheCacheStartPrimingJobCommandDef(AuthOptionPack auth) : CommandDef
{
    public override string Name => "start-priming-job";
    protected override bool IsDestructive => true;

    public readonly ResourceGroupOptionPack ResourceGroup = new();
    public readonly RenderOptionPack Render = new();

    /// <summary>Name of cache. Length of name must not be greater than 80 and chars must be from the [-0-9a-zA-Z_] char class.</summary>
    [CliOption("--cache-name", Required = true)]
    public partial string? CacheName { get; }

    /// <summary>Schema for the name of resources served by this provider. Note that objects will contain an odata @id annotation as appropriate. This will contain the complete resource id of the object. These names are case-preserving, but not case sensitive.</summary>
    [CliOption("--priming-job-name", Required = true)]
    public partial string? PrimingJobName { get; }

    /// <summary>The URL for the priming manifest file to download. This file must be readable from the HPC Cache. When the file is in Azure blob storage the URL should include a Shared Access Signature (SAS) granting read permissions on the blob.</summary>
    [CliOption("--priming-manifest-url", Required = true)]
    public partial string? PrimingManifestUrl { get; }

    /// <summary>Supply the full request body as a JSON string (overrides individual options).</summary>
    [CliOption("--body-json")]
    public partial string? BodyJson { get; }

    /// <summary>Do not wait for the long-running operation to complete.</summary>
    [CliOption("--no-wait")]
    public partial bool NoWait { get; }

    private readonly AuthOptionPack _auth = auth;

    protected override async Task<int> ExecuteAsync(CancellationToken ct)
    {
        var log = DiagnosticOptionPack.GetLog(ParseResult);
        var client = new AzureRestClient(_auth.GetCredential(log), log);
        var armClient = new ArmClient(_auth.GetCredential(log));
        var (resolvedSub, resolvedRg, resolvedName) = await ResourceNameResolver.ResolveAsync(
            CacheName!, ResourceGroup, armClient, "Microsoft.StorageCache/caches", ct);
        var path = $"/subscriptions/{resolvedSub}/resourcegroups/{resolvedRg}/providers/Microsoft.StorageCache/caches/{resolvedName}/startPrimingJob";

        var body = BodyJson is { } rawJson
            ? JsonNode.Parse(rawJson)!.AsObject()
            : new JsonObject
            {
                ["primingJobName"] = JsonValue.Create(PrimingJobName),
                ["primingManifestUrl"] = JsonValue.Create(PrimingManifestUrl),
            };
        var httpResp = await client.SendRawAsync(HttpMethod.Post, path, "2026-01-01", body, ct);
        if (!NoWait)
        {
            var result = await LroPoller.PollAsync(httpResp, client, "2026-01-01", log, ct);
            await Render.GetRendererFactory().CreateRendererForType(typeof(System.Text.Json.Nodes.JsonNode))
                .RenderAsync(System.Console.Out, result, ct);
        }
        return 0;
    }
}
