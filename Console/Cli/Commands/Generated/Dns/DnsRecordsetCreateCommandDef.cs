// <auto-generated/>
#nullable enable
#pragma warning disable CS1591
using System.Text.Json.Nodes;
using Console.Cli.Http;
using Console.Cli.Shared;
using Console.Rendering;
using Azure.ResourceManager;

namespace Console.Cli.Commands.Generated;

/// <summary>Creates or updates a record set within a DNS zone.</summary>
/// <remarks>Creates or updates a record set within a DNS zone.</remarks>
public partial class DnsRecordsetCreateCommandDef(AuthOptionPack auth) : CommandDef
{
    public override string Name => "create";
    protected internal override bool IsDestructive => true;

    public readonly ResourceGroupOptionPack ResourceGroup = new();
    public readonly RenderOptionPack Render = new();

    /// <summary>The name of the DNS zone (without a terminating dot).</summary>
    [CliOption("--zone-name", Required = true)]
    public partial string? ZoneName { get; }

    /// <summary>The name of the record set, relative to the name of the zone.</summary>
    [CliOption("--relative-record-set-name", Required = true)]
    public partial string? RelativeRecordSetName { get; }

    /// <summary>The type of DNS record in this record set. Record sets of type SOA can be updated but not created (they are created when the DNS zone is created).</summary>
    [CliOption("--record-type", Required = true)]
    public partial string? RecordType { get; }

    private readonly AuthOptionPack _auth = auth;

    protected override async Task<int> ExecuteAsync(CancellationToken ct)
    {
        var log = DiagnosticOptionPack.GetLog();
        var cred = _auth.GetCredential(log);
        var client = new AzureRestClient(cred, log);
        var armClient = new ArmClient(cred);
        var (resolvedSub, resolvedRg, resolvedName) = await ResourceNameResolver.ResolveAsync(
            ZoneName!, ResourceGroup, armClient, "Microsoft.Network/dnsZones", ct);
        var path = $"/subscriptions/{resolvedSub}/resourceGroups/{resolvedRg}/providers/Microsoft.Network/dnsZones/{resolvedName}/{RecordType}/{RelativeRecordSetName}";

        var result = await client.SendAsync(HttpMethod.Put, path, "2018-05-01", null, ct);
        await Render.GetRendererFactory().CreateRendererForType(typeof(System.Text.Json.Nodes.JsonNode))
            .RenderAsync(System.Console.Out, result, ct);
        return 0;
    }
}
